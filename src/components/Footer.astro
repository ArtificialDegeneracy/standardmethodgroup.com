---
const companyLinks = [
  { label: 'Home', href: '/' },
  { label: 'How We Work', href: '/how-we-work' },
  { label: 'Who We Help', href: '/who-we-help' },
  { label: 'Never Miss a Call', href: '/never-miss-a-call' },
  { label: 'About', href: '/about' },
  { label: 'Contact', href: '/contact' },
];

const legalLinks = [
  { label: 'Privacy Policy', href: '/privacy' },
];
---

<footer class="footer">
  <div class="footer-inner">
    <div class="footer-col footer-brand">
      <span class="footer-wordmark">Standard Method Group</span>
      <p class="footer-blurb">An operations partner for Tampa Bay small businesses. We build systems that handle the routine work, so you can get back to running the business.</p>
      <a href="/contact" class="footer-cta">Start a conversation &rarr;</a>
    </div>

    <div class="footer-col">
      <h4 class="footer-heading">Company</h4>
      <ul class="footer-list">
        {companyLinks.map(link => (
          <li><a href={link.href}>{link.label}</a></li>
        ))}
      </ul>
    </div>

    <div class="footer-col">
      <h4 class="footer-heading">Contact</h4>
      <ul class="footer-list">
        <li><a href="mailto:dan@standardmethodgroup.com">dan@standardmethodgroup.com</a></li>
      </ul>
    </div>

    <div class="footer-col">
      <h4 class="footer-heading">Legal</h4>
      <ul class="footer-list">
        {legalLinks.map(link => (
          <li><a href={link.href}>{link.label}</a></li>
        ))}
      </ul>
    </div>
  </div>

  <div class="footer-bottom">
    <p class="footer-service-area">Serving Tampa Bay &amp; Pinellas County, FL</p>
    <p class="footer-copyright">&copy; {new Date().getFullYear()} Standard Method Group. All rights reserved.</p>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-navy);
    padding: var(--space-8) 0 var(--space-5);
  }

  .footer-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 var(--space-3);
    display: grid;
    grid-template-columns: 1.6fr 1fr 1fr 1fr;
    gap: var(--space-5);
  }

  .footer-brand {
    max-width: 320px;
  }

  .footer-wordmark {
    display: block;
    font-family: var(--font-heading);
    font-weight: var(--font-weight-heading);
    font-size: 1.0625rem;
    color: var(--color-white);
    margin-bottom: var(--space-2);
  }

  .footer-blurb {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.6;
    margin-bottom: var(--space-2);
  }

  .footer-cta {
    font-family: var(--font-heading);
    font-weight: var(--font-weight-heading);
    font-size: 0.875rem;
    color: var(--color-white);
    text-decoration: none;
  }

  .footer-cta:hover {
    color: var(--color-white);
    text-decoration: underline;
  }

  .footer-heading {
    font-family: var(--font-heading);
    font-weight: var(--font-weight-heading);
    font-size: 0.875rem;
    color: var(--color-white);
    margin-bottom: var(--space-2);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .footer-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .footer-list a {
    font-family: var(--font-heading);
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.8);
    text-decoration: none;
    transition: color 200ms ease;
  }

  .footer-list a:hover {
    color: var(--color-white);
  }

  .footer-bottom {
    max-width: var(--max-width);
    margin: var(--space-5) auto 0;
    padding: var(--space-3) var(--space-3) 0;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    text-align: center;
  }

  .footer-service-area {
    font-family: var(--font-heading);
    font-size: 0.8125rem;
    color: rgba(255, 255, 255, 0.65);
    margin-bottom: var(--space-1);
  }

  .footer-copyright {
    font-family: var(--font-heading);
    font-size: 0.8125rem;
    color: rgba(255, 255, 255, 0.65);
  }

  @media (max-width: 900px) {
    .footer-inner {
      grid-template-columns: 1fr 1fr;
    }

    .footer-brand {
      grid-column: 1 / -1;
      max-width: none;
    }
  }

  @media (max-width: 768px) {
    .footer-inner {
      grid-template-columns: 1fr;
      gap: var(--space-3);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .footer-list a {
      transition: none;
    }
  }
</style>
